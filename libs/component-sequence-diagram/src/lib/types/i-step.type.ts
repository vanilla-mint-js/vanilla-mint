
export interface IStep {
  with?: string|string[];
  from?: string;
  to?: string;
  internally?: string;
  the?: string;
  because?: string;
  will?: string;
  withJson?: object;
  if?: string;
  viaUrl?: string;
}